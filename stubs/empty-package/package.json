{
  "name": "empty-package",
  "version": "0.0.0",
  "private": true,
  "description": "Local stub used by pnpm-workspace.yaml overrides to drop TypeScript-only dependencies (@types/*) that no plain-JS consumer ever imports at runtime, rather than adding an external package just to remove others."
}
